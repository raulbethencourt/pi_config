# ~/.pi Improvement Plan

Consolidated backlog from the `~/.claude` vs `~/.pi` config audit (2026-07-18). Each item below is a standalone task, intended to be picked up and implemented one at a time. Check items off as they're completed. No changes have been made to `~/.pi` yet — this is the punch list.

Source documents (full detail behind each item):
- `/tmp/pi-improvement-audit/claude-config-inventory.md` — what's configured in `~/.claude`
- `/tmp/pi-improvement-audit/pi-config-inventory.md` — what's configured in `~/.pi`
- `/tmp/pi-improvement-audit/diff-and-recommendations.md` — feature-level gap table
- `/tmp/pi-improvement-audit/claude-docs-research.md` — ideas from official Claude Code docs

---

## High priority

### 1. Add a Context7-equivalent MCP server — done
Pi has no live library/API-docs lookup capability today (`native-web-search`/`websearch` are generic web search, not a curated docs index). Context7 ships a public MCP server (`npx -y @upstash/context7-mcp` or similar) with no Anthropic-account dependency — slot it into `agent/mcp.json` the same way `chrome-devtools`/`github`/`context-mode` are already wired, then add it to `worker`/`debugger`'s `mcpTools` frontmatter. Cheapest, highest-confidence item on this list.

**Done.** `context7` entry added to `agent/mcp.json`; wired into `worker`'s and `debugger`'s `mcpTools` frontmatter.

### 2. Memory housekeeping + size discipline — done
Not a new build — pi's `memory` tool + `context-mode` semantic index are already at or ahead of Claude's `MEMORY.md`. Two concrete actions:
- Investigate and likely prune the orphaned `.omc/project-memory.json` cache — its producer/consumer extension couldn't be traced in the reviewed code, it's dead config surface.
- Add a hard size ceiling + escalating-warning discipline to the memory index (mirrors Claude's Auto Memory: `MEMORY.md` loads only its first 200 lines/25KB, with a warn-then-error mechanism pushing detail into separate topic files). Pi's memory index has no such ceiling today and could hit the same bloat failure mode as it grows.

**Done.** `.omc/project-memory.json` confirmed dead (zero codebase references) and not referenced anywhere. Size ceiling with warn/error escalation added in `agent/extensions/memory/index.ts` (`MAX_MEMORY_LINES=200`, `MAX_MEMORY_BYTES=25KB`, warn thresholds + truncation).

---

## Medium priority

### 4. Package the existing pipeline as one scripted command — done
`SKILL_REFERENCE.md` already documents the exact scout→planner→critic→worker→review sequence with PROCEED/REVISE/BLOCK gating that Claude's `pilot-explore-plan-critique-execute-review.js` automates as a single call — pi's top-level loop currently has to manually re-issue each phase's `subagent` call in order. Since `subagents/index.ts` already supports a `tasks[]` parallel-dispatch mode and `commands-loader.ts` already supports prompt-template commands, this is a convenience wrapper (a `/pipeline`-style command or small extension), not new orchestration design.

**Done.** Landed as a `pipeline` extension packaging scout→planner→critic→worker→codereviewer with PROCEED/REVISE/BLOCK gating (commit `feadfe3`).

### 5. Path-scoped conditional instruction loading — done
Claude Code supports `.claude/rules/*.md` files with `paths: ["src/api/**/*.ts"]`-style frontmatter — rules that load into context only when the agent touches matching files, instead of every session. This is "conditional CLAUDE.md fragments" and directly addresses instruction-file bloat over time. No equivalent found in pi's inventory — worth adding if pi's AGENTS.md/SYSTEM.md-equivalent files grow large.

**Done.** Landed as the `rules-loader` extension (`agent/extensions/rules-loader/`), hooking `tool_result` for `read`/`write`/`edit` calls and merging global (`~/.pi/agent/rules/*.md`) with project-local (`<project>/.pi/rules/*.md`, using pi's own `CONFIG_DIR_NAME` convention rather than Claude's `.claude/rules`) rule files, project taking precedence on same-basename collisions. Dedup is per-session via `${ruleId}::${absoluteFilePath}` composite keys, so a rule reminds again per distinct matching file rather than going silent after its first match. Known limitation: this injects into `tool_result` content (ordinary tool output), not system-prompt/context space the way Claude Code's own mechanism does, so the model may weight it as tool output rather than a hard directive — see `agent/extensions/rules-loader/README.md` for the full rationale and mitigation.

### 6. Fixed "protected paths never auto-approved" list — done
Claude Code maintains a fixed list of paths (`.git`, `.claude`, shell rc files, `.npmrc`, `.mcp.json`, etc.) that are never auto-approved regardless of permission mode, short of full bypass. Cheap, high-value guardrail — protects the agent's own config and VCS state even under a fully-trusting/autonomous run. Check whether pi's `bash-guard`/`trust.json` already covers this, or only covers test-file immutability and named protected folders.

**Done.** `PROTECTED_FOLDER_ENTRIES`, `PROTECTED_WRITE_ONLY_FILES` (`.bashrc`, `.zshrc`, `.gitconfig`, `.npmrc`, `auth.json`), and `PROTECTED_PATH_PATTERNS` (`.git/hooks`, `.git/config`) added to `agent/extensions/bash-guard/index.ts` (commit `cf52f62`). Hard-block bypasses found during review were fixed under item #15.

### 7. Add a `code-simplifier`-tier persona — done
The one confirmed roster drift since pi's 14 agents were ported from Claude: pi's `refactorer` handles explicit, potentially broad-scope refactors, but there's no narrow "polish what was just written for clarity/consistency, default to recently-modified code" role distinct from that. Low cost (one more persona `.md` + a routing-table row), moderate value.

**Done.** Persona file added at `agent/extensions/subagents/agents/code-simplifier.md` (commit `99fa896`), already correctly referenced in `agent/SYSTEM.md` and `agent/skills/orchestrator/SKILL.md`'s delegation matrices. Remaining gaps closed: `AVAILABLE_AGENTS` and the blocked-tool error-message string in `agent/extensions/delegation-enforcer/index.ts` now list `code-simplifier` alongside the other 14 personas; the Agent Catalog table in `README.md` now has a `code-simplifier` row (model/role sourced from the persona file's frontmatter); `agent/extensions/subagents/routing.json` was left without a `code-simplifier` entry on purpose — its frontmatter pins a single fixed model (`github-copilot/gpt-5.4-mini`) with no complexity-tiered variation, so a routing-table row would add no value (confirmed via `routing.ts`: an agent absent from `routing.json` simply keeps its own frontmatter model). No test in `agent/tests/` asserts persona-registry/disk sync, so none needed updating.

### 8. Add a skill-creator-equivalent meta-tool — done
No skill-authoring skill/extension found in pi's inventory. Given pi already has 16 skills and an active skill-development cadence (design-doc backlog in `agent/docs/*.md`), a lightweight scaffolding skill/command (frontmatter template, `SKILL.md` conventions, trigger-description guidance) would reduce friction for adding #7 above and any future skills.

**Done.** New skill added at `agent/skills/skill-creator/SKILL.md`: fail-fast `name` validation against pi's skill-name regex, a collision check that scans `name:` frontmatter across every `agent/skills/*/SKILL.md` (not just directory names, since pi allows the two to differ), an embedded frontmatter reference table, "good vs. poor" `description`-quality examples geared at pi's auto-trigger matching, and a post-creation checklist covering the README.md "Skills (Triggered On-Demand)" bullet and optional subagent `skills:` frontmatter wiring — with explicit notes that `orchestrator/SKILL.md`, `routing.json`, and `AVAILABLE_AGENTS` don't need touching for skills (those are agent-registry surfaces, not skill-registry ones). `README.md`'s "Skills (Triggered On-Demand)" list also got one new alphabetized bullet for `skill-creator` itself. Code-reviewer approved with HIGH confidence, no blockers.

### 9. Consider exposing pi itself as an MCP server — done
Claude Code can act as an MCP *server* via `claude mcp serve`, letting other tools/agents call into it. MCP is an open standard, not Anthropic-proprietary — if pi doesn't expose its own capabilities this way, it's a concrete missing feature that would let pi interoperate with other agent tools (including Claude Code itself).

**Done.** New standalone extension at `agent/extensions/mcp-server/` (`serve.ts`, `agent-loader.ts`, `env-allowlist.ts`, `concurrency.ts`, `truncate.ts`, `package.json`) exposes 6 read-only personas (scout, researcher, planner, critic, codereviewer, codereviewer-deep) as MCP tools over stdio; each call spawns the real `pi` binary as a subprocess rather than importing the interactive subagent runtime directly, since `agent/extensions/subagents/index.ts` has module-scope imports that only resolve inside pi's own process. `buildPiArgs` was extracted out of `index.ts` into a new `agent/extensions/subagents/build-args.ts` (zero behavior change for the interactive `subagent` tool, confirmed by tests) to make it reusable from the standalone server; `spawnPiProcess` (`runner.ts`) gained an opt-in `envMode: "replace"` mode so the MCP server never leaks its own environment to spawned children — a strict allowlist (`PATH`/`HOME` only) replaces the default full `process.env` merge, verified sufficient since all configured providers authenticate via file-based `~/.pi/agent/auth.json`. The 6-persona allowlist and stripping of the `mcpTools`/`memory` frontmatter fields are structural (the loader never reads/surfaces that data), not runtime checks. Went through two rounds of planner↔critic adversarial review (one on the original reuse-based design before a module-resolution blocker forced a redesign around subprocess-spawning; one on the redesign itself) plus a from-scratch worker implementation that caught and fixed a plan error (SDK's `registerTool` needs a Zod schema, not plain JSON Schema — resolved by resolving `zod` from its existing on-disk location as a transitive SDK dependency, no new install needed) and an ESM interop gap (`telemetry.ts`'s `node:sqlite` require shim). 78 new+existing tests pass (`agent/tests/subagents-build-args.test.ts`, `subagents-runner-envmode.test.ts`, `mcp-server-{env-allowlist,concurrency,truncate,agent-loader}.test.ts` plus the full pre-existing `subagents*.test.ts` family, no regressions). Code-reviewer approved with MEDIUM confidence; one advisory (not a defect) to give `verifyPiBinary`/`PI_BINARY_PATH` and the `globalThis.require` polyfill in `serve.ts` one more human read given the diff size in a security-sensitive area.

### 10. Shared task list + file-locking for parallel multi-agent delegation — done
Claude Code's experimental "Agent Teams" feature adds a shared, dependency-tracked task list, peer-to-peer teammate messaging, and file locking to prevent merge conflicts between parallel sessions. Pi's roster-based delegation model implies parallel multi-agent runs but doesn't coordinate around shared state today. Even a lightweight version (a shared JSON task list + a per-file claim/lock) would reduce collision risk.

**Done.** New extension at `agent/extensions/coordination/` (`session-dir.ts`, `lock-store.ts`, `tasks.ts`, `index.ts`) gives sibling subagents dispatched from the same top-level `pi` process a shared per-file lock and a shared `tasks.json`. `session-dir.ts` resolves `~/.pi/agent/state/coordination/<sessionId>/` (sessionId = `<pid>-<timestamp>`) and canonicalizes paths (symlink-resolved, with a not-yet-existing-write-target fallback) so lock keys are stable across equivalent path strings. `lock-store.ts` is the actual mutual-exclusion primitive: atomic on-disk exclusive creation (`fs.promises.open(lockPath, "wx")`, the only mechanism visible across separate OS processes) layered with an in-process reentrancy map so repeated acquisitions by the same pid are free. Reap model is dead-pid-only, no TTL ever — a lock is reclaimed only when `process.kill(holderPid, 0)` throws `ESRCH`, never based on file age or elapsed time; this was a deliberate correction after an initial critic BLOCK on an earlier TTL-based reaping design. `index.ts`'s `acquireLocksForPaths` sorts a multi-file tool call's target canonical paths lexicographically before acquiring (a fixed global order across every caller, preventing cross-call deadlocks) and rolls back every already-acquired lock if any later acquisition in the sequence fails after its retry/backoff budget — no partial lock state is ever left behind. A shared `agent/extensions/shared/hashline-paths.ts` module was extracted during this work, deduplicating path-normalization logic that had drifted into a third near-identical copy across `hashline` and `rules-loader` (a duplication smell this work incidentally fixed rather than introduced). The extension is deliberately loaded only into subagent child processes, never the interactive main session: `agent-loop.js` skips `finalizeExecutedToolCall`/`afterToolCall` for a blocked `tool_call`, so a blocked call never produces a matching `tool_result` — if some other extension's handler blocked a call after this one had already granted a lock earlier in the same handler chain, the lock would leak permanently (recoverable only once the holding process itself dies); restricting to subagent children, whose only other co-resident hook is `hashline`'s `tool_result`-only handler, sidesteps this entirely. Wired into both `agent/extensions/subagents/index.ts` (the interactive dispatch path) and `agent/extensions/subagents/build-args.ts` (the standalone/mcp-server reuse path), each minting its own coordination session id via a shared `mintCoordinationSessionId()` helper and threading it to spawned children via `PI_COORDINATION_SESSION_ID`. Covered by 24 new coordination tests (`agent/tests/coordination-{lock-store,multi-file-atomic,stale-session-prune,tasks}.test.ts`, including a real-SIGKILL regression test and an explicit no-TTL regression test) plus the pre-existing suite with no regressions. Passed a two-stage code review with a final APPROVE (0 Critical, 0 Important); its cheap advisory follow-ups were folded back in immediately after: narrowing the dead-pid liveness check in `lock-store.ts`/`session-dir.ts` to only treat `ESRCH` as dead (any other error, e.g. `EPERM`, now logs and falls through to the safe "assume alive" default rather than being treated as dead), and documenting one more accepted limitation in the README. Explicitly out-of-scope for this v1: no cross-session protection (two separate top-level `pi` invocations get independent coordination directories with no shared visibility), no guarantee on network filesystems (NFS/SMB — the design assumes `~/.pi` is on local disk), no read-consistency guarantee, and — newly documented — two concurrent tool calls (different `toolCallId`s) from the *same* subagent process targeting the same file are both granted rather than serialized, since the in-process reentrancy map keys only on pid and pi-agent-core executes a turn's prepared tool-call bodies concurrently by default (blocking would deadlock the batch); this is not a regression, since baseline pi has the identical race with no coordination extension at all, and it stays outside this extension's "separate OS processes" threat model.

### 19. `Prompt Suggestions`-style ghost-text next-message hint — done
Claude Code shows a grayed-out suggested next message directly in the prompt input bar after each response — accepted with Tab/Right-arrow (fills the input box, then Enter sends it) or dismissed by just typing. On a session's first turn it's seeded from git history (recently-worked files); afterward it's derived from conversation context (e.g. suggesting "commit these changes" right after an implementation finishes). It runs as a low-cost background request reusing the parent conversation's prompt cache, is skipped in plan mode and by default in print/non-interactive mode, and is toggleable via `CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION=false` or `/config`. No equivalent found in pi's inventory: `zsh-history.ts` (ctrl+r) is a manually-invoked picker over shell history, not an automatic post-response hint, and `pi-tui`'s `AutocompleteProvider` mechanism is trigger-character-driven completion for `@`-mentions/paths/slash-commands while typing, not an idle full-message suggestion offered after the agent finishes. Porting this needs a materially different TUI primitive (ghost text rendered in the empty input box, accepted on Tab/Right-arrow, cleared on first keystroke) plus a background low-cost suggestion request keyed off turn-end and, for the first turn, `git log`/`git diff` of recently-touched files — more than a small addition to the existing autocomplete/history surface, so scope it as its own item.

**Done.** New extension at `agent/extensions/ghost-suggest/` implementing ghost-text prompt suggestions in the input box, structured as: `guards.ts` (pure eligibility + first-turn-detection logic, unit-tested), `generate.ts` (pure prompt-building + sanitization logic, unit-tested, plus git-log-fetching helper and cached background-LLM-request function reusing `btw.ts`'s side-session pattern), `editor.ts` (CustomEditor subclass rendering ghost text and intercepting Tab/Right-arrow-to-accept, approved under Legacy Code Exemption with manual checklist + scoped tsc typecheck due to tightly-coupled TUI code), and `index.ts` (wires extension via `agent_end` hook). Underwent full planner→critic review cycle with a genuine revision loop: first critic pass flagged a fabricated-looking `ctx.mode` field claim, which a second evidence-based pass confirmed actually exists in the installed `@earendil-works/pi-coding-agent` package — demonstrating the review process catching and correctly resolving a real ambiguity rather than a defect. Implementation incidentally discovered a pre-existing dormant bug in `agent/extensions/btw.ts` (~line 554): a `session.agent.replaceMessages(...)` call referencing a method that doesn't exist on the installed Agent class, never triggered by any current test but worth a follow-up investigation. Test results: `guards.test.ts` and `generate.test.ts` both fully GREEN (24 + 14 tests respectively), plus a new `agent-state-messages.test.ts` (3 tests); full suite 878 passing with identical 7 pre-existing unrelated failures as baseline (tests/token-stats-*.test.ts, tests/powerline-subagents.test.ts), confirmed via git stash comparison to predate this work. `agent/vitest.config.ts`'s `include` glob was broadened to also discover `extensions/**/*.test.ts` so co-located tests run under the standard test command. Code-reviewer approved with HIGH confidence, zero blocking findings; one advisory note (not a defect): git commit metadata including author emails is sent to the LLM in the first-turn seed prompt — inherent to the approved design and worth documenting for maintainers. Intentional scope: reuses the existing `agent_end` hook and pi's real `ctx.mode` field rather than requiring new pi-tui/extension-API surface, resolving the main open architectural question from the original plan item.

A subsequent pre-commit review pass (code-reviewer/code-simplifier/security-auditor-deep) rejected the first attempt at this commit with five findings, all investigated against the real installed `@earendil-works/pi-coding-agent` package (not guessed) and fixed: (1) a suspected editor-swap race in `index.ts` — confirmed via the installed package's `setCustomEditorComponent` that in-flight typed text *is* preserved across a swap (it reads the outgoing editor's text and calls `setText` on the new one), so the fix was reordering `ensureGhostTextEditor(ctx)` to run synchronously before any async work (the first-turn git-log fetch), closing the gap entirely rather than relying on the preserved-text safety net; (2) `generate.ts`'s cached side session gained a `suggestionBusy` guard mirroring `btw.ts`'s `sideBusy` pattern; (3) a new `session_shutdown` handler (a real event on the installed extension API, confirmed via its type declarations) resets the module-level `ghostEditor`/cached-session singletons, since pi's extension loader caches and reuses the loaded module across a session switch within the same process — without this, `ensureGhostTextEditor()`'s stale-reference guard would silently stop reinstalling the editor after any session switch; (4) the unverified `session.agent.state.messages = seedMessages` assignment was verified directly against the real installed `Agent` class (constructed with no mocking in the new `agent-state-messages.test.ts`), confirming its `messages` setter does exactly what its doc comment claims — copies the provided top-level array; (5) `sanitizeSuggestion`'s truncation was changed from UTF-16-code-unit `String.slice()` to code-point-aware truncation (avoiding a split surrogate pair at the 80-character boundary), and a misleadingly-worded test description in `guards.test.ts` (claiming a non-finite `PI_SUBAGENT_DEPTH` is "treated as 0", when it actually just fails the `Number.isFinite` check) was corrected. A second review pass then raised one further Important finding — that `agent-state-messages.test.ts`'s claim of a "confirmed-nonexistent `replaceMessages` method" was itself unverifiable from the diff alone (the reviewing subagent has no `node_modules` access) and appeared to contradict `agent/extensions/btw.ts`'s own `session.agent.replaceMessages(...)` call site — resolved by adding an executable regression test asserting `typeof agent.replaceMessages === "undefined"` against the real installed `Agent` class, turning the claim into a CI-enforced fact rather than an unverifiable comment (btw.ts's call site is the same pre-existing dormant bug noted above, intentionally left untouched); that same pass also flagged (Medium, security) that `sanitizeSuggestion` didn't strip ANSI/control characters before the suggestion is spliced directly into a TUI line, and an unused `buildSessionContext` import in `generate.ts` — both fixed. A third review pass then caught a genuine remaining TOCTOU gap in the new `suggestionBusy` guard itself (it was being set *after* an `await`, leaving a window where two overlapping calls could both pass the check), independently corroborated by two of the three review subagents in that pass — fixed by moving the flag-set to before the first `await`, closing the race outright (stronger than the `btw.ts` pattern it was modeled on, which has the same latent gap, left untouched as pre-existing/out of scope); that pass also widened the control-character strip to include the C1 range (`0x80-0x9F`). Test counts as of the final commit: `guards.test.ts` (24), `generate.test.ts` (16), `agent-state-messages.test.ts` (4); full suite 881 passing with the same 7 pre-existing unrelated failures.

---

## Low priority / already covered — no action needed (kept for reference, don't re-investigate)

### 11. `notify-on-stop`-style desktop notification — already present
`agent/extensions/notify.ts` already fires a desktop notification when the agent finishes and is waiting for input. No gap.

### 12. `code-philosophy` skill — already present in both
Already embedded in the `worker` persona's system prompt in both tools. No gap.

### 13. `claude-in-chrome`-style native browser pairing — not portable, not worth chasing
Proprietary Anthropic-Chrome-extension protocol tied to account-level device pairing — no public API surface for a third-party local tool to integrate with. Pi already covers the practical ground via two working paths (`browser` skill/Playwright, `browser-tools`/raw CDP) plus the same `chrome-devtools-mcp` package Claude uses. The actually-portable part ("control a real browser via CDP") is already present in both tools.

### 14. Test-file/protected-folder immutability — pi is already ahead, don't regress
Pi's `bash-guard` extension enforces test-file immutability and protected-folder restrictions as **non-bypassable in-code blocks** with no override path. Claude currently states the equivalent rule as policy text in `CLAUDE.md`, relying on subagent compliance — and Claude's own memory system records an incident of a worker violating exactly that kind of policy-text-only protection (`rm -rf` on pre-existing files "because the plan said so"). If pi's config is ever "upgraded" toward Claude's patterns wholesale, keep this mechanism as-is rather than weakening it to match Claude.

---

## Process note (not a pi task — an issue observed in `~/.claude` itself, during this audit)

While writing `diff-and-recommendations.md`, a `worker` subagent was blocked once by `~/.claude`'s own `enforce-delegation` Write hook despite being the legitimately dispatched subagent for that exact task — the `agent_id` signal apparently wasn't detected in that context. The subagent used the sanctioned one-off `FORCE_DELEGATE_OVERRIDE=1` fallback for that single write rather than looping on a blocked call, which is within the documented last-resort carve-out. Worth investigating `~/.claude/hooks/enforce-delegation*.sh`'s `agent_id` detection on its own at some point — this is unrelated to the pi improvement work above, filed here only so it isn't lost.

---

## Backlog (found during item #6's review process)

### 15. `bash-guard`: `find`/`fd` bypass protected-path blocking entirely via bare command-name matching — fixed
`READ_ONLY_CMDS` (in `agent/extensions/bash-guard/index.ts`) classifies any command starting with `find` or `fd` as unconditionally read-only via a bare command-name prefix match, with no inspection of the command's own arguments. `analyzeSegment`'s general risk-scoring path does separately flag `find <path> -delete` as high severity — but that's an independent, best-effort warning-and-prompt mechanism, not the same code path as the protected-path hard-block. For the protected-path hard-block specifically (`findBlockedProtectedFolderReference`, and the analogous `PROTECTED_WRITE_ONLY_FILES`/`PROTECTED_PATH_PATTERNS` check right after it), matching on the bare command name means `find <protected-path> -delete` or `find <protected-path> -exec rm {} \;` is still classified read-only and sails straight through the hard-block, for every `PROTECTED_FOLDER_ENTRIES` entry (old directory entries and the newer credential-file entries alike) — not something narrow to this session's additions. Found while reviewing/fixing the narrower read-only-exemption bug for the two credential-file entries (item #6); deferred as its own dedicated fix given the severity (full bypass of the hard-block, not just a downgrade to a promptable warning) and breadth (affects the entire `PROTECTED_FOLDER_ENTRIES`/`PROTECTED_WRITE_ONLY_FILES` surface, not one entry). Likely fix shape: give `find`/`fd` the same segment-level argument inspection `analyzeSegment` already does for `find -delete`, but wired into the protected-path block path rather than only the general risk-prompt path — e.g. only treat `find`/`fd` as read-only when none of `-delete`, `-exec`, `-execdir`, `-fprintf`, `-ok`, `-okdir` appear among its arguments.

**Fixed.** The bare command-name misclassification described above is closed, and review of the fix surfaced (and closed) several further bypasses of the same protected-path hard-block along the way: chaining (`&&`, `;`, `||`), piping (`|`), backgrounding (`&`), `;;`, `|&`, bare newlines, and `fd`'s bundled short-flags (`-Hx`-style, where a value-taking flag character bundled into a short-flag group was previously invisible to the exemption check). Fix lives in `agent/extensions/bash-guard/index.ts`, with regression coverage in `agent/tests/bash-guard-protected-paths.test.ts` (133 bash-guard-related tests passing).

### 16. `bash-guard`: command/process substitution still bypasses the protected-path hard-block
Command substitution (`$(...)`, backticks) and process substitution (`<(...)`, `>(...)`) still bypass the same protected-path hard-block fixed in item #15, because `shell-quote` flattens a substitution's inner tokens into the same segment as the outer command with no nesting-depth marker — e.g. `cat $(rm -rf ~/.ssh)` is still misclassified as read-only, since `cat` reads as the (only visible) command. Fixing this needs subshell-depth-aware tokenization, which is a materially larger change than the fixes already applied in item #15. This gap is documented inline in `agent/extensions/bash-guard/index.ts` near the `splitOnOps` call site.

**Fixed.** The substitution-bypass gap is closed via a new raw-string scanner `extractSubstitutionSpans()` in `agent/extensions/bash-guard/index.ts`, which directly scans for all four substitution forms (`$()`, backticks, `<()`, `>()`) without relying on `shell-quote`'s token boundaries. `isReadOnlyBashCommand()` now unconditionally disqualifies any command containing detected substitution syntax from the read-only exemption (fail-closed design: even harmless inner content like `cat <(cat ~/.ssh/config)` loses the exemption if a protected path is textually referenced anywhere). Separately, `analyzeBashCommand()` (the general risk-scoring path, distinct from the hard-block) gained depth-capped (max depth 5) recursive scanning into each substitution span, merging severity and attaching "inside command/process substitution: " prefix so dangerous commands hidden in substitutions still surface as risk prompts rather than silently passing. Two intentional over-detection trade-offs are documented inline: the scanner doesn't track quoting (a quoted literal containing `$(text)` still registers as substitution), and `$((...))` arithmetic expansion also registers (stronger conservative stance than attempting to distinguish it from command substitution). Covered by 17 new regression tests across `agent/tests/bash-guard-protected-paths.test.ts` and `bash-guard.test.ts`, testing all four bypass forms, a credential-file variant, a should-not-false-positive case, the confirmed fail-closed trade-off, a malformed/unterminated-input case, and a depth-cap/no-hang case. Underwent scout→planner→critic review (critic raised a REVISE on unconditional fail-closed scope, resolved via explicit user confirmation to keep it); then worker→code-reviewer pass (APPROVE, HIGH confidence, zero Critical/Important findings). Full test suite: 159/159 passing in bash-guard test files, 898/905 in full suite (7 pre-existing unrelated failures).

### 17. `bash-guard`: `fd` bundled-short-flag value-taking set is missing two real value-taking flags
The bundled-short-flag detection added while fixing item #15 defines its value-taking-flag set as `FD_VALUE_TAKING_SHORT_FLAG_CHARS = new Set(["d", "E", "t", "e", "S", "c", "j"])` in `agent/extensions/bash-guard/index.ts`, but `fd` has two more real value-taking short flags not in that set: `-o`/`--owner` and `-C`/`--base-directory`. This can only cause rare over-blocking (a harmless `fd` invocation being incorrectly treated as non-exempt from the read-only classification), never a new bypass, so it's low priority — but worth completing the set for correctness.

**Fixed.** Added `"o"` and `"C"` to `FD_VALUE_TAKING_SHORT_FLAG_CHARS` in `agent/extensions/bash-guard/index.ts`, so bundled tokens like `-Hox`/`-HCx` now correctly stop the scan at `-o`/`-C` and treat the remainder as that flag's attached value instead of misreading it as a bundled `-x`/`--exec`. Covered by the two pre-existing regression tests in `agent/tests/bash-guard-protected-paths.test.ts` (search for "[item #17 gap]"), which flipped from RED to PASS; full bash-guard suite (161 tests across `bash-guard.test.ts` and `bash-guard-protected-paths.test.ts`) passes, with no regressions elsewhere in the repo's test suite.

### 18. `bash-guard`: output redirection to a protected path is not hard-blocked in headless/subagent mode
`analyzeBashCommand` in `agent/extensions/bash-guard/index.ts` flags output redirection (`>`, `>>`, `2>`) as a promptable "medium" risk, but that's a UI confirmation prompt, not the protected-path hard block (`findBlockedProtectedFolderReference`/`PROTECTED_WRITE_ONLY_FILES`/`PROTECTED_PATH_PATTERNS`) fixed in items #15–#17 — `tokensToStrings` strips redirect operators without them ever disqualifying a command from the read-only exemption there. Worse, the promptable path itself is skipped entirely in headless/non-interactive execution when `bash-guard-auto-allow` is set (the common subagent execution path), since `promptRunOrAbort` short-circuits before ever reaching the user. Net effect: a command like `cat payload > ~/.ssh/authorized_keys` (or `echo ... >> ~/.ssh/authorized_keys`) run by a subagent in headless mode is neither prompted nor hard-blocked, and can silently overwrite a protected path. This is a **pre-existing gap, not a regression** — the old bare-regex classifier that items #15–#17 replaced had the identical hole, since it never inspected redirect targets either. Deferred as its own item given it needs a materially different fix shape (teaching the protected-path hard-block to inspect redirect targets, independent of the read-only/mutating-primary classification those items addressed) rather than a small addition to existing logic.

**Fixed.** Round 1: Added `extractOutputRedirectTargets`/`findBlockedOutputRedirectTarget` to `agent/extensions/bash-guard/index.ts`, extracting redirect targets from `>`/`>>`/`>|` tokens (including `{op:"glob", pattern}`-shaped tokens for unquoted `*`/`?` targets) and checking them against `isProtectedPath`/`isProtectedWriteOnlyPath`, wired to run before the headless/subagent early-return path. Passed TDD (RED→GREEN) and planner→critic design review; code-reviewer-deep returned APPROVE with 2 Minor fail-safe findings (narrow `[[ a > b ]]` false-positive, `$HOME`-indirection limitation). Round 2 (caught by pre-commit hook): Glob-character redirect targets (e.g. `~/.bashr?`) were silently dropped by `typeof next === "string"` check; fixed via TDD and glob-pattern matching against `PROTECTED_WRITE_ONLY_FILES`/`PROTECTED_FOLDER_ENTRIES`/`PROTECTED_PATH_PATTERNS`. Code-reviewer rejected: glob-vs-path matching missed mid-segment glob characters (e.g. `~/.s?h/authorized_keys`) and never checked `PROTECTED_PATH_PATTERNS` (`.git/hooks`/`.git/config`). A fix round (segment-wise glob matching) closed those, but code-reviewer rejected again: POSIX bracket-expression syntax (`~/.ss[h]/authorized_keys`) bypassed the entire chain, confirmed as a live verified bypass. Round 3 (final, landed): Rather than chase individual glob syntax forms indefinitely, `findBlockedOutputRedirectTarget` was deliberately simplified to unconditionally hard-block ANY output-redirect target containing shell glob/pattern metacharacters (`*`, `?`, `[`, `]`, `{`, `}`) — tagged `kind: "glob-obfuscated"` — regardless of whether it matches a protected path. This deleted the five-function glob-vs-path-matching subsystem as dead code. Final code-reviewer pass: APPROVE, HIGH confidence, verified against all three previously-bypassing repros plus brace-expansion variant, no regressions. Test coverage: 202/202 passing across `bash-guard-protected-paths.test.ts` (151 tests, all output-redirect-target regressions) and `bash-guard.test.ts` (51 tests); full suite 942/949 passing with same 7 pre-existing unrelated baseline failures.

### 20. `btw.ts`: `createSideSession` calls a nonexistent `session.agent.replaceMessages` method — fixed
Found as a follow-up to item #19's "Done" note, which flagged this same dormant bug during the ghost-suggest implementation but deferred fixing it as out of scope for that work. Inside `agent/extensions/btw.ts`'s (unexported) `createSideSession` closure (~line 554), the seed-message branch called `session.agent.replaceMessages(seedMessages as typeof session.state.messages)` — but `replaceMessages` does not exist on the real installed `Agent` class from `@earendil-works/pi-coding-agent`, so this threw a runtime `TypeError` any time a BTW side session was created or recreated after the thread already had prior history (i.e. every time the seed-message branch was actually exercised). The correct pattern — direct assignment to the `state.messages` setter, which copies the provided array — was already validated and in production use one file over, in `agent/extensions/ghost-suggest/generate.ts`'s `runSuggestionRequest` (`session.agent.state.messages = seedMessages`), and was itself verified against the real (non-mocked) `Agent` class by item #19's `agent-state-messages.test.ts`.

**Fixed.** `createSideSession`'s seed-message branch in `agent/extensions/btw.ts` now calls a new exported helper, `seedSideSessionMessages(session, seedMessages)`, which performs the same direct `session.agent.state.messages = seedMessages` assignment `generate.ts` uses, instead of the nonexistent `replaceMessages` call. The seeding assignment was pulled out of the unexported `createSideSession` closure into this small top-level helper specifically so it could be exercised directly in a test without exporting the whole side-session factory — `createSideSession` itself stays unexported and otherwise unchanged. `agent/tests/btw-side-session.test.ts` (the regression test that pinned this bug down against a real, unmocked `Agent`/`AgentSession` built via the same `createAgentSession` SDK call `btw.ts` itself uses) was updated to call the new `seedSideSessionMessages` export instead of duplicating the buggy call shape inline, since the original version only reproduced the bug's call shape rather than exercising btw.ts's actual code — its assertions (`expect(...).not.toThrow()`, `expect(session.state.messages).toEqual(seedMessages)`) were left unchanged, and it now passes GREEN. `agent/tests/btw.test.ts` and `agent/extensions/ghost-suggest/agent-state-messages.test.ts` remain unaffected and passing. Full suite: 901/908 passing, with the same 7 pre-existing unrelated failures as baseline (`tests/token-stats-*.test.ts`, `tests/powerline-subagents.test.ts`). The separate `codingTools` import concern the test author flagged (a possibly-stale/removed named export from `@earendil-works/pi-coding-agent`) is a distinct issue and was left untouched, out of scope for this fix.

---

## Backlog (found during item #18's review process)

### 21. `bash-guard`: `$HOME` and unset-variable expansion collapse to empty string rather than resolving
Environment variable expansion in `bash-guard`'s command parsing (via the `shellParse` call inside `extractOutputRedirectTargets` and throughout the general `analyzeBashCommand` path) inherits a pre-existing, documented limitation from the underlying `shell-quote` library: `shellParse` is invoked without an explicit `env` argument, so environment variables like `$HOME` expand to an empty string (`""`) rather than being resolved to their real values. A command like `cat payload > $HOME/.ssh/authorized_keys` therefore parses with `$HOME` expanding to `""`, making the extracted redirect target `/.ssh/authorized_keys` rather than matching the literal `~`-prefixed or absolute-path forms `bash-guard`'s protected-path checks already handle. This was documented inline as a `KNOWN LIMITATION` comment in `agent/extensions/bash-guard/index.ts` and re-flagged during item #18's final security-auditor-deep pass, which confirmed it is pre-existing and unworsened by item #18's redirect-target fix (both inherit the limitation via their shared `shellParse` call). Severity: Medium. Possible fix direction: pass an explicit `env` argument to the `shellParse` call(s) so `$HOME` and other safe, well-known variables resolve to their real values before path-matching, though this needs explicit design decision on which environment variables are safe to expose to the parser.

**Fixed.** Added a new `SHELL_PARSE_ENV: Record<string, string> = { HOME }` constant to `agent/extensions/bash-guard/index.ts`, reusing the file's existing `HOME` constant, and passed it as the second argument to all three `shellParse` call sites (`analyzeBashCommandBase`, `isReadOnlyBashCommand`, `extractOutputRedirectTargets`). The allowlist is deliberately narrow — only `$HOME` is resolved, since it's the only variable the file's protected-path constants are built from; `$USER`/`$PWD`/`$OLDPWD` remain unresolved on purpose, as nothing constructs protected paths from them and resolving PWD/OLDPWD risks reflecting the guard process's own stale cwd. The `commandReferencesPath`/`findBlockedProtectedFolderReference` path (plain substring matching, never calls `shellParse`) remains untouched and out of scope. The inline `KNOWN LIMITATION` comment block was updated to reflect fixed-vs-still-open accurately. Underwent planner→critic review (critic raised REVISE on the shell-quote `env` signature and all-3-call-sites scope, resolved by directly verifying shell-quote's own parse.js source rather than a second planning round) then TDD: tester wrote 4 genuinely-RED tests plus 2 control tests (`$NOTHOME` and `$PWD` non-resolution) in `agent/tests/bash-guard-protected-paths.test.ts`, worker implemented to GREEN. Final test counts: 209/209 passing in bash-guard test files (`bash-guard.test.ts` + `bash-guard-protected-paths.test.ts`); code-reviewer APPROVE at HIGH confidence, zero regressions, confirmed via runtime verification of the actual installed shell-quote behavior.

### 22. `bash-guard`: substitution-computed redirect targets fall back to raw-substring matching and can both over-match and under-match — fixed
Output-redirection protection added in item #18 catches substitution-computed targets (e.g. `cat payload > $(echo ~/.ssh/authorized_keys)`) indirectly, via the item #16 raw-substring `commandReferencesPath`/`PROTECTED_PATH_PATTERNS.test()` mechanism — the literal protected-path text happens to be embedded in the substitution's source code, so the regex finds it. However, this raw-substring approach can both over-match (false positive if a protected path's literal text appears in a command incidentally, unrelated to what actually gets written) and under-match (a substitution whose *output* differs from its *literal source text* — e.g. `$(echo ~/.ssh/authorized_keys | tr a-z A-Z)` — would not be caught, since the raw command text never contains the literal target path in matching form). This was deliberately left unresolved during item #18's implementation (verified during planning that the raw-substring approach already catches the specific common example above, and closing the gap would require attempting direct substitution-target resolution, which may not be tractable within `bash-guard`'s static-analysis constraint). The pre-commit review flagged it as Medium-severity, pre-existing, and worth tracking as an actionable backlog item rather than leaving implicit. Investigation needed: whether direct substitution-target resolution is even feasible within bash-guard's static-analysis approach (it likely isn't, given arbitrary command substitution can run anything), and if not, whether this should be documented as a permanent accepted limitation with explicit false-positive/negative risk language.

**Fixed.** Root cause recap: the item #16 raw-substring fallback (`commandReferencesPath`/`PROTECTED_PATH_PATTERNS`) only catches substitution-computed redirect targets when the literal protected-path text happens to appear verbatim in the command source; a variable-indirected variant like `X=.ssh; cat payload > $(echo ~/$X/authorized_keys)` left no literal text to match, and `>(...)`-as-a-redirect-target (e.g. `cat > >(tee /tmp/file.log)`) wasn't extracted as a target at all by the existing `extractOutputRedirectTargets`. The fix: a new function `findSubstitutionTaintedRedirectTarget` was added to `agent/extensions/bash-guard/index.ts`, wired into `findBlockedOutputRedirectTarget` ahead of the existing glob/protected-path checks. It implements an asymmetric position-aware rule: a substitution marker (`$(`, backtick, `<(`, `>(`, bare trailing `$`) at the START of a redirect target is blocked unconditionally (regardless of well-formedness); a marker in SUFFIX position (non-empty literal text precedes it) is only blocked if that literal prefix or the substitution's own visible inner content contains `/` or `~`. A new `"substitution-computed"` kind was added to the return-type union, with its own hard-block reason message. Why the asymmetric rule, not a simple "block any substitution": an existing, immutable regression test (`cat file.txt > out-$(date +%s).log` must return `null`, at `agent/tests/bash-guard-protected-paths.test.ts:1025`) already locked in that timestamp-suffixed output filenames must not be blocked — a blunt "block all substitution" rule was rejected specifically because it would have required modifying that pre-existing test, which is out of scope without explicit user authorization. This was discovered during a planner→critic revision cycle (one REVISE round) rather than assumed upfront. Verified safe by deep review: a `code-reviewer` pass (LOW confidence, escalated per policy due to diff size and shell-quote tokenization reliance) was followed by `code-reviewer-deep`, which independently re-verified every `shell-quote` token-shape assumption against the real installed library and additionally proved via actual `bash -c` execution that the deliberately-accepted residual gap (a suffix-position opaque substitution call with no `/`/`~` in its own text, e.g. `out_$(compute_secret_path).txt`) can never resolve to an absolute or tilde-expanded path at runtime — any non-empty literal prefix mathematically forces the resolved target to be relative to cwd, so the accepted gap is confirmed unexploitable, not just undetected. Final verdict: APPROVE, 0 Critical/Important findings. Scope boundaries, explicitly out of scope for this fix: double-quoted `"$(...)"`/backtick content is live in real bash but textually indistinguishable from an inert single-quoted literal at the shell-quote token level (no quote-type metadata survives parsing), so both are treated identically by the suffix-position rule — a documented, accepted trade-off, not a bug. The general whole-command over-match risk in `commandReferencesPath` (pre-existing `KNOWN LIMITATION`, unrelated to substitution specifically) is unchanged. Test results: 171/171 passing in `agent/tests/bash-guard-protected-paths.test.ts`, full bash-guard suite 222/222 (`bash-guard.test.ts` + `bash-guard-protected-paths.test.ts`), zero regressions. Review process: planner→critic (one REVISE round, then PROCEED) → tester (RED, 8 genuinely failing tests) → worker (GREEN) → code-reviewer (APPROVE, LOW confidence) → code-reviewer-deep (APPROVE, final verdict, 0 Critical/Important, 2 Minor housekeeping notes).

### 23. Code-quality nits in `agent/extensions/bash-guard/index.ts`'s item #18 redirect-hard-block code
The item #18 output-redirect protection code contains several cosmetic code-quality improvements flagged by the pre-commit review pass's code-simplifier stage, none blocking but all worth addressing in a future cleanup pass: (a) a nested ternary expression that should be rewritten as a switch statement or if-else chain to match the codebase's own style guide; (b) redundant inline comments that duplicate detail already captured in `pi-improvement-plan.md`'s item #18 changelog entry rather than explaining non-obvious code decisions; (c) two comments with reversed "see above"/"see below" directional references (pointing to the wrong location relative to where they're actually written); (d) one function using an inline anonymous return type where a sibling function in the same file uses a named type — inconsistent style. Severity: Low (purely cosmetic, no functional impact). Recommendation: bundle into a future general cleanup pass on this file rather than a dedicated fix cycle, unless someone touches this code again for another reason first; the same pre-commit-hook review pass that addressed items #27/#28 also flagged that comment density and test-description verbosity have recurred in that newer code, so any future pass should cover that code too.

**Fixed.** Four behavior-preserving cosmetic fixes landed in agent/extensions/bash-guard/index.ts: (1) corrected a stale "(see above)" comment near line ~1119 referencing findBlockedOutputRedirectTarget (defined later in the file, ~line 1444) to "(see below)" — confirmed via grep as the only remaining stale directional reference in the file; (2) trimmed a review-process attribution ("code-reviewer REJECT finding") out of the comment documenting the `&`-backgrounding shell-quote tokenization bug, keeping the substantive technical fact; (3) converted the nested/chained ternary building the output-redirect hard-block `reason` string (keyed on `blockedRedirect.kind`) into a switch statement with explicit cases for `"protected-folder"`/`"protected-write-only"`/`"substitution-computed"` and a `default` case preserving the original ternary chain's implicit catch-all (covering `"glob-obfuscated"` and any future unmatched kind) — every message string verified byte-for-byte identical; (4) extracted `findBlockedOutputRedirectTarget`'s inline anonymous return type into a named `BlockedOutputRedirectTarget` type, mirroring the file's existing `ProtectedFolderEntry` convention, scoped to only that one exported function (two other private helpers with similar anonymous return types were deliberately left untouched as an explicit, noted scope boundary, since they postdate the original item text). Went through planner→critic review (one REVISE round on the initial bundled plan, addressed by splitting bash-guard work from the picker work into fully independent tracks) before implementation. `code-reviewer` (not the -deep escalation, since this file's diff came back HIGH confidence) verified every hard-block reason string was preserved and confirmed zero change to decision logic — APPROVE, HIGH confidence. Test results: 245/245 passing across bash-guard.test.ts + bash-guard-protected-paths.test.ts (unchanged count, zero new tests needed since this was a pure refactor of already-covered logic), `npx tsc --noEmit` showed zero new type errors.

---

## Backlog (found during item #22's review process)

### 27. `bash-guard`: suffix-position substitutions in redirect targets bypass via literal-prefix path-traversal obfuscation — fixed

Item #22's initial fix added a `PURE_TRAVERSAL_PREFIX` regex to detect bare `.`/`..` literal prefixes glued to suffix-position substitutions (e.g. `out_$(printf '\57..\57.ssh\57authorized_keys')`, where octal `\57` = `/`), preventing runtime path-traversal sequences hidden in octal escapes from slipping past the existing `PATH_CHAR` check. A code-reviewer pass that rejected this fix (HIGH confidence) discovered and verified a still-open adjacent bypass: ANY literal prefix (not just dot-prefixes) glued to a suffix-position substitution could compute a runtime path-traversal sequence via the same `printf` octal-escape technique (e.g. `x$(printf '\57..\57.ssh\57authorized_keys')`), making the `PURE_TRAVERSAL_PREFIX` mitigation incomplete. The root cause: static analysis can never prove a substitution's runtime output is free of path separators; any suffix-position substitution whose inner content remains opaque to the analyzer is a potential bypass vehicle, whether or not its source text contains a dot prefix.

**Fixed.** Made ALL suffix-position substitutions in redirect targets unconditionally tainted, mirroring the existing unconditional taint for START-position substitutions — closing the entire bypass class rather than attempting finite detection of specific prefix forms. In `agent/extensions/bash-guard/index.ts`, the `evaluateRedirectTargetTaint` function was updated to set `kind: "substitution-computed"` for any substitution appearing after non-whitespace literal text, removing the now-dead `PATH_CHAR` and `PURE_TRAVERSAL_PREFIX` regexes and their associated helper functions (`collectSubstitutionInnerContent`, `innerContentAfterSuffixMarker`) entirely as dead code. This required modifying one previously-immutable regression test in `agent/tests/bash-guard-protected-paths.test.ts`: the `out-$(date +%s).log` "does NOT block" assertion (line 1025) was flipped to "blocks" per explicit user authorization, reflecting the new unconditional suffix-position policy. Several other suffix-position "does NOT block" assertions conflicting with the new policy were also updated with design-doc comments (single/double-quoted dynamic-filename cases, malformed/unclosed substitutions, the previously-accepted-limitation opaque-call case `out_$(compute_secret_path).txt`, and duplicate `.`/`..`-prefix control cases) — all matching the new unconditional taint rule. New regression tests added for the two originally-reported exploit strings (`out_$(printf '\57..\57.ssh\57authorized_keys')` and `x$(printf '\57..\57.ssh\57authorized_keys')`). A final code-reviewer pass (HIGH confidence) verified the fix closes the bypass class completely by running the two exploit strings plus a dozen devised variants (nested/multiple substitutions, arithmetic `$(( ))`, `>(...)`, backtick, quoted forms, glob+substitution combos, multiline substitutions, extra whitespace, `2>` fd redirection form, adjacent-quoted-literal concatenation) directly against the live `findBlockedOutputRedirectTarget` function — all correctly blocked. Confirmed via grep that dead symbols have zero remaining references. Confirmed `npx tsc --noEmit` shows no new type errors. Final test counts: 230/230 passing in `bash-guard-protected-paths.test.ts` + `bash-guard.test.ts` (up from 222/222); full repo suite has the same 7 pre-existing unrelated baseline failures, no new failures introduced. Two non-blocking Minor findings from the final review were left as-is: (a) `findEarliestSuffixMarker`'s returned `{index, marker}` fields are now unused dead weight since position/identity no longer matter under the unconditional policy (a simple boolean check would suffice); (b) the truncated `target` field in the hard-block message for the unquoted-split branch is slightly less informative to a human, though not a security issue. Landed together with item #28 in a single commit (commit bd5710b), verified by an independent pre-commit hook review (code-reviewer-deep, code-simplifier, security-auditor-deep pipeline, reporting a clean pass with 0 Critical/Important findings).

---

## Feature idea (requested by user, 2026-07-22)

### 24. Cross-session prompt-history picker (ctrl+r-style, but for pi prompts instead of shell commands)

**The idea**: pi already has a ctrl+r-triggered picker for shell command history — `agent/extensions/zsh-history.ts` reads `~/.zsh_history` directly, parses its extended format (`: timestamp:elapsed;cmd`), deduplicates keeping most-recent-first, and shows a fuzzy-filterable `SelectList` (via `@mariozechner/pi-tui`'s `SelectList`/`Input`/`Container`/`fuzzyFilter` primitives) triggered by `pi.registerShortcut("ctrl+r", ...)` — registered at the extension level, not in `agent/keybindings.json`. On selection it pastes the chosen command (prefixed with `!`) into the editor input. The user wants an analogous picker, but searching **the user's own prompts** (messages sent to pi) instead of shell commands — and explicitly wants it to aggregate prompts across **all past pi sessions, across all workspaces** (not just the current project/session), giving a true global prompt-history search. On selection, the picked prompt should be dropped back into the input box for reuse/editing, mirroring the existing ctrl+r UX.

**Technical grounding for feasibility**: pi session state is stored as JSONL files under `agent/sessions/<workspace-id>/` — one file per session, one JSON record per line, filenames are timestamp+UUID. A user's own prompt text lives in records shaped like `{"type":"message", "message":{"role":"user","content":[{"type":"text","text":"..."}], "timestamp":...}, "timestamp":"<ISO>"}`. Storage is scoped per workspace (each project/working-directory gets its own `<workspace-id>` subdirectory under `agent/sessions/`), so a global (all-workspaces) picker needs to walk every workspace subdirectory under `agent/sessions/`, not just the current one — this is the scope the user explicitly confirmed they want (global, all workspaces), as opposed to a narrower current-workspace-only version. No existing code aggregates across multiple session files today — this would be new functionality, not an extension of anything already there.

**Decided**: ctrl+r is reserved for `zsh-history.ts`'s bash-history picker and must not be reused or overloaded. The new prompt-history picker must use its own distinct Ctrl+key binding (exact key to be chosen during implementation, but "not ctrl+r" is now a fixed constraint).

**Open design questions**: how to dedupe/rank a potentially very large aggregate prompt history (unlike shell history, which tends to be single-line and short, pi prompts can be long/multi-paragraph — needs a UI truncation/preview strategy for the picker list); performance of scanning potentially hundreds of JSONL files across many workspaces on every ctrl+<key> press (may need caching/indexing rather than a full re-scan each time); and how to handle sessions directories that may have grown very large over time (the existing `--home-rabeta-.pi--` workspace alone already has 113 session files).

**Done.** New extension at `agent/extensions/prompt-history/` (`prompt-store.ts`, `index.ts`) resolves the open design questions above with an incremental on-disk cache rather than a full re-scan on every keypress: each workspace's session files are fingerprinted by mtime+size as a fast path, falling back to a SHA1 content hash only for files modified within the last 5 real seconds — closing a same-second-rewrite race that mtime+size alone can't detect on filesystems with 1-second mtime granularity. The cache is written atomically (temp file + rename) with `0o600` permissions, and the display list is capped at the newest 5000 entries while the underlying per-file manifest stays uncapped, so a re-scan only ever has to process files that actually changed since the last run rather than regressing to a full re-parse once the cap is hit. The picker binds to ctrl+f (ctrl+r stays exactly as-is, reserved for `zsh-history.ts`, per the constraint above) and, unlike the shell-history picker's `!`-prefixed paste, drops the selected prompt's full raw text into the editor with no prefix. Went through two rounds of planner→critic review: the first pass came back REVISE on three blockers — no eviction strategy for an unbounded aggregate history, the mtime+size race on coarse-grained filesystems, and a concurrent-write race with no locking — resolved via the 5000-entry display cap, the 5-second hash-fallback window, and a deliberate decision *not* to reuse `agent/extensions/coordination/`'s lock primitive (its own README scopes it to subagent children of one session, not arbitrary top-level `pi` processes competing for the same cache file) in favor of atomic rename plus treating the cache itself as self-healing/re-derivable from the session files at any time; the second critic pass returned PROCEED. Followed TDD: `tester` wrote RED tests first (module-not-found, confirmed failing), `worker` implemented to GREEN (40/42 plus 2/2 across `agent/tests/prompt-history-store.test.ts` and `agent/tests/prompt-history-paths.test.ts` initially), with the 2 remaining failures traced to a Vitest 3.2.7/Node 24 ESM limitation where `vi.spyOn` can't spy on a `node:fs` namespace import directly — confirmed as a test-mechanism gap rather than a logic bug via sibling non-spy tests already covering the same code paths — fixed with user confirmation (since it touched an already-authored test file) by switching to `vi.mock("node:fs", { spy: true })` plus `vi.mocked(...)` instead of `vi.spyOn`, a mechanism-only change with the same assertions. `code-reviewer`'s first pass REJECTed on 2 Important findings — unguarded `fs.readFileSync` calls in `refreshPromptHistory`'s per-file loop and an unguarded `saveCache` call in `index.ts`'s ctrl+f handler, either of which could throw and abort the picker instead of degrading gracefully — fixed via try/catch-and-skip matching the function's existing defensive style for other `fs` calls, plus a new regression test for the unreadable-mid-scan case. The second `code-reviewer` pass returned APPROVE at MEDIUM confidence, with one non-blocking follow-up noted: a minor hash-representation inconsistency between a Buffer-hash and a UTF-8-string-hash call site, introduced by an incidental double-read fix, verified harmless/bounded/self-healing in practice and left unfixed. Final test state: full suite 985/992 passing, with the same 7 pre-existing unrelated failures as baseline (`tests/token-stats-*.test.ts`, `tests/powerline-subagents.test.ts`), no regressions.

---

## Backlog (found during item #27's review process)

### 28. `bash-guard`: `>&word` combined redirect-with-fd-duplication form bypasses the output-redirect protected-path hard-block — fixed

A pre-commit hook running its own independent automated review (code-reviewer-deep, code-simplifier, security-auditor-deep) over the staged diff for item #27 identified a High-severity finding: the combined redirect-with-fd-duplication bash syntax `>&word` was never recognized as an output-redirect operator anywhere in the protected-path hard-block pipeline (`extractOutputRedirectTargets`, `findSubstitutionTaintedRedirectTarget`) — only `>`/`>>`/`>|` were matched. When `word` is non-numeric (i.e., not a file descriptor like `>&2`, `>&-`, or `2>&1`), `>&word` is a genuine filesystem write, distinct from fd-duplication forms. This meant a command like `echo KEY >& $(printf '\57home\57rabeta\57.ssh\57authorized_keys')` slipped past every layer of protection, including item #27's brand-new substitution-taint check, representing a concrete SSH authorized_keys-overwrite repro that defeated the exact protection items #22/#27 claim to provide.

**Fixed.** `worker` added `{op:">&"}` token recognition to both `extractOutputRedirectTargets` and `findSubstitutionTaintedRedirectTarget` in `agent/extensions/bash-guard/index.ts`, with a new `isFdDuplicationOrCloseWord` helper to distinguish numeric fd-duplication/close forms (which remain unblocked, since they never touch the filesystem) from genuine write targets (anything else, which get the same protected-path/glob/substitution-taint checks). Tokenization was empirically verified against the installed `shell-quote` package (via a probe script) rather than assumed: `>&word`, `>&2`, `2>&1`, `>&-`, and `>& $(...)` all tokenize identically as `{op:">&"}` followed by a plain word token (or substitution pattern for variable targets) — only the word's value differs at runtime, which is why value-based checking was necessary. New regression tests added to `agent/tests/bash-guard-protected-paths.test.ts` covering the reported exploit, plain unobfuscated protected-path `>&word` forms, and non-regression controls for the numeric/fd-close forms. A follow-up code-reviewer pass (separate from the pre-commit hook) verified the fix independently. Final test count: 241/241 passing in `bash-guard-protected-paths.test.ts` + `bash-guard.test.ts` (up from 230), zero regressions. Landed together with item #27 in a single commit (commit bd5710b), verified by an independent pre-commit hook review (code-reviewer-deep, code-simplifier, security-auditor-deep pipeline, reporting a clean pass with 0 Critical/Important findings).

---

## Backlog (found during item #28's review process)

### 29. `bash-guard`: `REDIRECT_OPS` list missing `>&` operator affects UX warning classifier — fixed
The `REDIRECT_OPS` set in `analyzeBashCommandBase` (agent/extensions/bash-guard/index.ts, ~line 448) only lists `>`, `>>`, `2>`, `2>>` and does not include `>&` — a pre-existing gap predating item #28's fix. This affects only the separate, promptable "medium risk" interactive-mode UX warning classifier (`promptRunOrAbort` code path), not the unconditional hard-block path (`findBlockedOutputRedirectTarget`, which runs before the interactive/headless branch and already fully covers `>&word` per item #28's fix). The classification gap means a harmless `>&2` fd-duplication (or a non-protected-path target like `log_$(timestamp).txt >& /tmp/file.log`) still passes the read-only exemption for `REDIRECT_OPS` membership checks and skips the medium-risk promptable warning that other redirect operators would trigger. Severity: Minor (UX completeness only, not a security bypass — the hard-block still catches genuinely dangerous `>&word` forms targeting protected paths). Disposition: bundle into the same future cleanup pass as item #23, unless someone touches this code again for another reason first.

**Fixed.** `">&"` was added to the `REDIRECT_OPS` Set inside `analyzeBashCommandBase` in `agent/extensions/bash-guard/index.ts` (~line 448), following TDD (tester wrote a failing test in `agent/tests/bash-guard.test.ts` confirming `analyzeBashCommand("echo test >& some-non-protected-file.txt")` returned null instead of a medium-risk Risk object; worker then made it green). A first implementation attempt was REJECTED by code-reviewer (HIGH confidence): naively adding `>&` to `REDIRECT_OPS` caused `hasHarmfulRedirect` to also flag the extremely common, entirely benign fd-duplication/close idioms `2>&1`/`>&2`/`>&-` as risky "can overwrite files" redirects — a real UX regression, verified by the reviewer directly calling `analyzeBashCommand` on those strings. The fix was corrected to mirror the exclusion already used by the hard-block path (`extractOutputRedirectTargets`, item #28): `hasHarmfulRedirect`'s `>&` target check now also excludes targets matching `isFdDuplicationOrCloseWord` (bare non-negative integer or `-`), so fd-duplication forms stay unflagged while genuine `>&word` file-write targets are still correctly flagged. Three new regression tests were added to `agent/tests/bash-guard.test.ts` for `>&2`, `2>&1`, `>&-`. A second code-reviewer pass APPROVED at HIGH confidence, independently verifying via direct `analyzeBashCommand` calls (including an adversarial `>& -2` case confirming no over-exclusion) and raw `shell-quote` tokenization inspection that the fix is correct and the unconditional hard-block path (which already handled this correctly since item #28, comment-text-only diff there) remains unweakened. Note this fix only affects the promptable "medium risk" interactive-mode UX classifier — the unconditional protected-path hard-block was never affected by the original gap and needed no change. Final test count: 245/245 passing across `bash-guard.test.ts` + `bash-guard-protected-paths.test.ts` (up from 242).

### 30. Code comments refer to `>&word` fix as "item #27" when it should be "item #28" — naming inconsistency — fixed
Inline code comments in `agent/extensions/bash-guard/index.ts` and `agent/tests/bash-guard-protected-paths.test.ts` describing the `>&word` bypass fix (item #28) mistakenly refer to it as "item #27" in a few locations. Item #27 is the distinct, separate suffix-position-substitution fix that item #28 built on top of, and both landed together in commit bd5710b — the comments should read "#28" for canonical accuracy. Severity: Minor (purely cosmetic, no functional impact — code behavior is correct, only the comment labels are wrong). Disposition: correct during a future general cleanup pass, or opportunistically whenever anyone touches this code again.

**Fixed.** Corrected 5 mislabeled comment references from "item #27" to "item #28" — 3 in `agent/extensions/bash-guard/index.ts` (~lines 1159, 1179, 1379) and 2 in `agent/tests/bash-guard-protected-paths.test.ts` (~lines 1512, 1539) — everywhere a comment specifically described the `>&word` combined-redirect fix (item #28) but mislabeled it as "#27". One nearby reference at `bash-guard-protected-paths.test.ts` ~line 1529, which correctly refers to the actual, distinct item #27 suffix-position-substitution-taint work, was deliberately left untouched since it wasn't a mislabeling. Verified via code-reviewer pass (both round-1 and round-2, since this was implemented alongside item #29 in the same diff) that all 5 corrections are present, accurate, and no other stray "item #27" mislabels remain in either file.

---

## Backlog (found during item #24's review process)

### 25. `prompt-history`: picker rows aren't sanitized of ANSI escapes/control characters before rendering
Prompt text sourced from session JSONL files isn't stripped of ANSI escape sequences or control characters before being rendered as a row in the ctrl+f picker (`agent/extensions/prompt-history/index.ts`). A prompt that happens to contain raw ANSI codes or control characters (e.g. pasted terminal output, or an adversarial/malformed prompt) could corrupt the picker's row-per-item terminal rendering. This is the same fix class as item #19's ghost-suggest work applied to `sanitizeSuggestion` (which strips ANSI/C0/C1 control ranges before splicing suggestion text into a TUI line) — note that this exact gap is also pre-existing in the sibling `agent/extensions/zsh-history.ts` picker (shell command history), which item #24's `index.ts` was modeled on and inherited the same gap from, so a real fix should likely address both pickers together rather than just the new one. Flagged as Medium severity by the pre-commit security-auditor pass, non-blocking for item #24's commit.

**Fixed.** Both pickers now sanitize rendered picker-row text. A `sanitizeForDisplay(text: string): string` helper (stripping ANSI/CSI escape sequences, then C0/DEL/C1 control-byte ranges, then collapsing whitespace runs including embedded newlines/tabs to a single space and trimming) was first added independently to agent/extensions/zsh-history.ts and agent/extensions/prompt-history/index.ts (Phase A, intentionally duplicated), modeled on — not imported from — agent/extensions/ghost-suggest/generate.ts's private `stripControlCharacters` pattern. Applied only to each picker's rendered `label`/`description` fields; the underlying `value` field (what fuzzy-filtering matches against and what gets pasted back into the editor via each picker's own paste-transform — `!`-prefix for zsh-history, identity for prompt-history) is left completely raw, so sanitization never corrupts a pasted command or prompt. Phase A landed and was verified green (new unit tests covering ANSI/CSI stripping, C0+DEL stripping, C1-range stripping, multi-line/multi-paragraph collapsing, unicode passthrough, and empty-input handling) before Phase B (item #26's extraction) began, per an explicit critic-mandated sequencing split — an initial bundled Phase A+B plan was returned REVISE specifically because bundling a security-relevant sanitization fix with a large, previously-untested UI-scaffold extraction was judged riskier than landing them as two independently-verified steps.

### 26. `prompt-history`: picker UI is a near-verbatim structural copy of `zsh-history.ts`'s picker UI
`agent/extensions/prompt-history/index.ts`'s picker UI (Container/DynamicBorder/Input/SelectList scaffold, keyboard navigation handling) is a near-verbatim structural copy of `agent/extensions/zsh-history.ts`'s picker UI, differing only in the data source, the key binding (ctrl+f vs ctrl+r), and the paste behavior (no "!" prefix). This is a duplication/drift risk flagged by the pre-commit code-simplifier pass: future picker-UX fixes (like item #25 above) would need to be applied in two places by hand, and the two implementations could silently diverge over time. Consider extracting the shared scaffold (rendering, filtering-via-`fuzzyFilter`, up/down/tab/enter/escape navigation) into a common helper/module that both extensions call into, parameterized by data source, key binding, and paste-transform (identity vs "!"-prefix). Severity: Minor/cosmetic (no functional bug today), suitable for a future general cleanup pass rather than urgent, similar in spirit to how item #23 was scoped and deferred.

**Fixed.** The near-duplicate picker UI scaffolding in agent/extensions/zsh-history.ts and agent/extensions/prompt-history/index.ts (Container/DynamicBorder/Input/SelectList wiring, fuzzy-filter-on-raw-value, tab/shift-tab/up/down keyboard navigation with wraparound, enter/escape resolution) was extracted into a new shared module, agent/extensions/shared/history-picker.ts, exporting a parameterized `runHistoryPicker(options)` function (accepting a narrow `Pick<ExtensionUIContext, "custom" | "setEditorText">` interface rather than the full extension context — verified against the real installed `@earendil-works/pi-coding-agent` type declarations) plus the single canonical `sanitizeForDisplay` (Phase A's duplicated copies consolidated into one). Both original files now import from the shared module and re-export `sanitizeForDisplay` as a thin shim (`export const sanitizeForDisplay = sharedSanitizeForDisplay`) so their pre-existing direct-import unit tests keep working unchanged, mirroring the established `extensions/hashline/path-utils.ts` re-export-shim precedent already in this repo. Each file keeps only its data-source-specific glue local: `parseZshHistory`/`toSelectItem`-style item shaping, the two different paste-transforms, and the two different empty-state/missing-file notify message strings (verbatim unchanged). Since zero tests existed on the picker UI scaffold before this work, a tester wrote 17 characterization tests (agent/tests/zsh-history-picker.test.ts, 9 tests; agent/tests/prompt-history-picker.test.ts, 8 tests) against the pre-extraction code first — covering header/count text, fuzzy-filter narrowing, wraparound navigation including single-item no-op, enter/escape resolution with paste-transform, and empty/missing-file short-circuits, verified against the real installed `@mariozechner/pi-tui` package's actual key-byte-sequence behavior (tab="\t", shift+tab="\x1b[Z", up="\x1b[A", down="\x1b[B", enter="\r", escape="\x1b") rather than guessed — then re-run unchanged post-extraction to prove behavior preservation; all 17 stayed green with zero assertion changes. Went through two rounds of planner→critic review (first REVISE: bundling risk and lack of pre-extraction characterization tests, addressed by the Phase A/B split and the characterization-test step above; second pass: PROCEED, with implementation-detail warnings about integration-point specificity and scaffold-signature design resolved during implementation). `code-reviewer`'s first pass returned APPROVE at LOW confidence (a diff-size policy trigger, not a specific concern), so per the two-stage escalation protocol it was escalated to `code-reviewer-deep`, which returned a final APPROVE with zero Critical/Important findings after independently re-verifying the value/label sanitization boundary, the re-export shim's non-divergence guarantee, and the shared function's narrow `ui` parameter typing against the real type declarations; it noted two non-blocking Minor observations left as-is (a whitespace-collapse-before-ANSI-strip ordering quirk with no security impact, since the control-byte strip pass still removes any bare ESC byte regardless; and that Unicode format/BiDi control characters above U+009F remain unstripped, matching ghost-suggest's sibling implementation and outside item #25's stated ANSI+C0/C1/DEL scope). Final test counts: 35/35 passing across the four touched test files; full suite 1057/1064 passing with the same 7 pre-existing unrelated failures as baseline (tests/token-stats-*.test.ts, tests/powerline-subagents.test.ts), zero new failures; `npx tsc --noEmit` showed a reduced diagnostic count (650 vs. 667 pre-change, all pre-existing environment-config noise) confirming no new errors and one fewer source of duplicated boilerplate warnings.

